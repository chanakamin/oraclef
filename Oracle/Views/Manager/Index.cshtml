@{
    ViewBag.Title = "Index";
    //Layout = "~/Views/Recipe/_LayoutRecipe.cshtml";
}

<div ng-controller="managerCtrl">
    <div ng-view="">

    </div>

    <div class="row">
        <!--the bottom bar-->
        <div class="col-xs-12">
            <nav class="navbar navbar-inverse navbar-fixed-bottom">
                <div class="container-fluid">
                    <div class="navbar-header">
                        <a class="navbar-toggle collapsed" data-toggle="collapse" data-target="#managerNavbar" aria-expanded="false">
                            <span class="sr-only">Toggle navigation</span>
                            <span class="glyphicon glyphicon-cutlery"></span>
                        </a>
                        <p class="navbar-brand"><span class="glyphicon glyphicon-home"></span></p>
                    </div>
                    <div class="collapse navbar-collapse" id="managerNavbar">
                        <ul class="nav navbar-nav">
                            <li><a href="#" ng-click="link('new')"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>{{::words.newres}}</a></li>
                            <li><a href="#" ng-click="link('recipes')" ><span class="glyphicon glyphicon-home" aria-hidden="true"></span>{{::words.reslist}}</a></li>
                            <li><a href="#" ng-click="link('')">{{::words.approve}}</a></li>
                            <li><a href="#" ng-click="logout()">{{::words.logout}}</a></li>
                        </ul>                                
                    </div>
                </div>
            </nav>
        </div>
    </div>
</div>

