@{
    Layout = "_LoginLayout.cshtml";  
 }

<div id="login" @*ng-controller="loginCtrl"*@>
 <div class="col xs-12 hidden-xs col-sm-3 " >
                <div class="well">
                    <p>Just click Enter to start.
                    </p>
                    <span class="glyphicon glyphicon-arrow-down"></span>
                </div>
                 <div class="well" id="notChrome">
                    <p>
                        Although we did our best to ensure that this site is compatible to all browsers, for best performance use Chrome. It is totally free! You can 
                        <a href="https://www.google.com/intl/en/chrome/browser/desktop/index.html">download Chrome</a> Chrome here.
                    </p>
                </div>
     <noscript>
         <div class="well">
             <p>
                 Make sure
                 <a href="http://activatejavascript.org/en/instructions/chrome">Javascript</a> is enabled.
             </p>
         </div>
     </noscript>
        </div>
<div class="col-xs-12 col-sm-9 col-md-6 col-lg-6" >
     <div ng-view="">

     </div>
</div>

 <nav id="welcomeMenu" class="col-xs-12 col-sm-5">
        <div>
            <ul class="vertical">
                <li><a href="#" ng-click="link('signup')"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>{{words.signup}}</a>
                    <ul >
                        <li>
                            <sign-up></sign-up>
                        </li>
                    </ul>
                </li>
                <li><a href="#" ng-click="link('signin')"><span class="glyphicon glyphicon-log-in"></span>{{words.login}}</a>
                    <ul>
                        <li>
                            <sign-in></sign-in>
                        </li>
                    </ul>
                </li>
                <li><a href="#" ng-click="link('guest')"><span class="glyphicon glyphicon-user"></span>{{words.guest}}</a>
                    <ul>
                        <li>
                            <div>
                                <div class="row">
                                    <h3>{{operation}}</h3>
                                    <p>{{message}}</p>
                                </div>
                                <div class="row">
                                    <button ng-click="link('/')">{{words.cancel}}</button>
                                    <button ng-click="guest()">{{words.guest}}</button>
                                </div>
                            </div>
                        </li>
                    </ul>   
                </li>
            </ul>            
        </div>     
    </nav>
</div>
<!--why doesn't this script work?-->
